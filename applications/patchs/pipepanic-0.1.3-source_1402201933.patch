Les fichiers binaires build/pipepanic-0.1.3-source.original/main et build/pipepanic-0.1.3-source/main sont différents.
diff -rupN build/pipepanic-0.1.3-source.original/main.c build/pipepanic-0.1.3-source/main.c
--- build/pipepanic-0.1.3-source.original/main.c	2006-05-16 22:11:29.000000000 +0200
+++ build/pipepanic-0.1.3-source/main.c	2014-02-15 20:52:46.000000000 +0100
@@ -172,8 +172,6 @@ int main(int argc, char *argv[]) {
 		return 1;
 	}
 
-	atexit(SDL_Quit);
-
 	#ifdef DEBUG
 	printf("Setting video mode %ix%i\n", xres, yres);
 	#endif
@@ -185,6 +183,10 @@ int main(int argc, char *argv[]) {
 		exit(1);
 	}
 	
+	SDL_Delay(1000);
+	SDL_Flip( screen);
+	atexit(SDL_Quit);
+	
 	#ifdef DEBUG	
 	printf ("Loading bitmaps\n");
 	#endif
@@ -707,7 +709,8 @@ void draw_game(void) {
 		}
 	}
 	
-	if (redraw != REDRAWNONE) SDL_Flip(screen);
+	//if (redraw != REDRAWNONE) 
+	                  SDL_Flip(screen);
 	
 	if (fpstest) {
 		frames++;
@@ -1092,6 +1095,7 @@ void manage_user_input(void) {
 							/* Process Help->Exit clicks */
 							if (game_mode == GAMESHOWHELP) {
 								if (((xres == 320 || xres == 640) && (mx >= xres - 5.6 * tilew) && (mx < xres - 4.4 * tilew) && (my >= yres - 0.75 * tileh) && (my < yres - 0.25 * tileh)) || ((xres == 240 || xres == 480) && (mx >= xres - 5.6 * tilew) && (mx < xres - 4.4 * tilew) && (my >= (BOARDH - 0.75) * tileh + 2 * tileh) && (my < (BOARDH - 0.25) * tileh + 2 * tileh))) {
+									quit = 1; // CLM
 									manage_help_input(SDLK_ESCAPE);
 								}
 							}
diff -rupN build/pipepanic-0.1.3-source.original/Makefile build/pipepanic-0.1.3-source/Makefile
--- build/pipepanic-0.1.3-source.original/Makefile	2006-05-16 22:11:29.000000000 +0200
+++ build/pipepanic-0.1.3-source/Makefile	2014-02-19 18:43:42.000000000 +0100
@@ -3,11 +3,16 @@ OBJECTS=main.o
 TARGET=pipepanic
 
 # Settings for x86.
-CC=gcc
-CFLAGS=-O2 -Wall -pedantic `sdl-config --cflags`
-LINK=gcc
+CC=$(T_ARCH)-gcc
+# CFLAGS=-O2 -Wall -pedantic `sdl-config --cflags`
+CFLAGS=-O2 -Wall -pedantic 
+LINK=$(T_ARCH)-gcc
 LDFLAGS=
-LIBS=`sdl-config --libs`
+LIBS=-L$(ARM_SYSROOT)/usr/lib -Wl,-rpath=-L$(ARM_SYSROOT)/usr/lib  -lSDL 
+
+install:
+	mkdir -p $(TOMDIST)/bin/$(TARGET)
+	cp $(TARGET) *.bmp $(TOMDIST)/bin/$(TARGET)
 
 all:
 	$(CC) $(CFLAGS) -c $(SOURCES)
@@ -16,5 +21,5 @@ all:
 .PHONY: clean
 
 clean:
-	rm $(OBJECTS)
-	rm $(TARGET)
+	rm -f $(OBJECTS)
+	rm -f $(TARGET)
diff -rupN build/pipepanic-0.1.3-source.original/Makefile.arm build/pipepanic-0.1.3-source/Makefile.arm
--- build/pipepanic-0.1.3-source.original/Makefile.arm	2006-05-16 22:11:29.000000000 +0200
+++ build/pipepanic-0.1.3-source/Makefile.arm	1970-01-01 01:00:00.000000000 +0100
@@ -1,22 +0,0 @@
-SOURCES=main.c
-OBJECTS=main.o
-TARGET=pipepanic
-
-# Settings for arm.
-CC=/opt/Embedix/tools/bin/arm-linux-gcc
-CFLAGS=-pipe -Wall -W -O2 -DNO_DEBUG -DQWS -I/opt/Qtopia/sharp/include -I/opt/Qtopia/sharp/include/SDL -D_REENTRANT -Dmain=SDL_main
-LINK=/opt/Embedix/tools/bin/arm-linux-gcc
-LDFLAGS=-L/opt/Qtopia/sharp/lib
-LIBS=-lqpe -lqte -lSDL -lSDLmain -lpthread
-
-all:
-	$(CC) $(CFLAGS) -c $(SOURCES)
-	$(LINK) $(LDFLAGS) $(OBJECTS) $(LIBS) -o $(TARGET)
-	zaurus/_buildipk
-	zaurus/_buildtgz
-
-.PHONY: clean
-
-clean:
-	rm $(OBJECTS)
-	rm $(TARGET)
diff -rupN build/pipepanic-0.1.3-source.original/Makefile.x86 build/pipepanic-0.1.3-source/Makefile.x86
--- build/pipepanic-0.1.3-source.original/Makefile.x86	1970-01-01 01:00:00.000000000 +0100
+++ build/pipepanic-0.1.3-source/Makefile.x86	2014-02-15 20:52:46.000000000 +0100
@@ -0,0 +1,21 @@
+SOURCES=main.c
+OBJECTS=main.o
+TARGET=pipepanic
+
+# Settings for x86.
+CC=gcc
+# CFLAGS=-O2 -Wall -pedantic `sdl-config --cflags`
+CFLAGS=-O2 -Wall -pedantic -I/usr/local/include
+LINK=gcc
+LDFLAGS=
+LIBS=-L/usr/local/lib -Wl,-rpath=/usr/local/lib  -lSDL 
+
+all:
+	$(CC) $(CFLAGS) -c $(SOURCES)
+	$(LINK) $(LDFLAGS) $(OBJECTS) $(LIBS) -o $(TARGET)
+
+.PHONY: clean
+
+clean:
+	rm $(OBJECTS)
+	rm $(TARGET)
diff -rupN build/pipepanic-0.1.3-source.original/pipe.sh build/pipepanic-0.1.3-source/pipe.sh
--- build/pipepanic-0.1.3-source.original/pipe.sh	1970-01-01 01:00:00.000000000 +0100
+++ build/pipepanic-0.1.3-source/pipe.sh	2014-02-15 20:52:46.000000000 +0100
@@ -0,0 +1,5 @@
+#! /bin/sh
+
+cd ../pipepanic-0.1.3-source
+./pipepanic
+
